class ChatRoom {
    constructor(id, createdDate, name, chatMessages, photoUrl){
        this.id = id;
        this.createdDate = createdDate;
        this.name = name;
        this.chatMessages = chatMessages;
        this.photoUrl = photoUrl
    }
}

export default ChatRoom;